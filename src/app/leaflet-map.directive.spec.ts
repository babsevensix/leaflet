import { LeafletMapDirective } from './leaflet-map.directive';

describe('LeafletMapDirective', () => {
  it('should create an instance', () => {
    const directive = new LeafletMapDirective();
    expect(directive).toBeTruthy();
  });
});
